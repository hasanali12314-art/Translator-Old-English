{
  "name": "translator-old-english",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-redirects.mjs && node scripts/sync-security-headers.mjs && astro build && node scripts/verify-seo.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
