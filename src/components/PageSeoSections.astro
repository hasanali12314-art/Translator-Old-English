---
import type { Section } from '../data/pageSeoContent';
export type { Section };

export interface Props {
  heading?: string;
  sections: Section[];
}

const { heading = 'Learn more', sections } = Astro.props;
---

<div class="info-block page-seo-sections">
  <p class="page-seo-heading">{heading}</p>
  {sections.map((s) => (
    <div class="page-seo-section">
      <h3>{s.title}</h3>
      {s.paragraphs.map((p) => (
        <p>{p}</p>
      ))}
    </div>
  ))}
</div>

<style>
  .page-seo-heading {
    font-family: var(--font-display);
    font-size: var(--fs-h3-h4);
    font-weight: 600;
    margin-bottom: 20px;
    color: var(--text-primary);
  }
  .page-seo-section {
    margin-bottom: 28px;
  }
  .page-seo-section h3 {
    margin-bottom: 12px;
  }
  .page-seo-section p {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 12px;
  }
</style>
