---
import '../styles/global.css';
import { SITE_LOCALES } from '../data/siteLocales';
import {
  SITE_URL,
  SITE_NAME,
  SITE_EMAIL,
  SITE_PHONE,
  SITE_PHONE_TEL,
  SOCIAL_LINKS,
  DMCA_URL,
  NAV_ITEMS,
  FOOTER_QUICK_LINKS,
  BLOG_POSTS,
  FOOTER_BLOG_SLUGS,
  DEFAULT_OG_IMAGE,
  OG_IMAGE_WIDTH,
  OG_IMAGE_HEIGHT,
} from '../data/siteConfig';
import { getPageKeywords } from '../data/pageKeywords';
import { sentenceCase } from '../utils/sentenceCase';

const footerBlogLinks = FOOTER_BLOG_SLUGS.map((slug) => {
  const post = BLOG_POSTS.find((p) => p.slug === slug)!;
  return { slug, label: post.footerLabel ?? sentenceCase(post.title.split(/[—:?]/)[0].trim()) };
});

export interface Props {
  title: string;
  description?: string;
  keywords?: string;
  canonical?: string;
  ogImage?: string;
  noindex?: boolean;
}
const {
  title,
  description = 'Translator Old English — translate modern English to Old English using AI. Early, Middle, Ancient, or Simple styles. Free, instant, and historically accurate.',
  keywords: keywordsProp,
  canonical,
  ogImage = DEFAULT_OG_IMAGE,
  noindex = false,
} = Astro.props;

function normPath(p: string) {
  if (!p || p === '/') return '/';
  return p.endsWith('/') ? p : `${p}/`;
}
const currentPath = normPath(Astro.url.pathname);
const isActive = (href: string) => currentPath === normPath(href);
const isHome = currentPath === '/';
const activeClass = (href: string) => (isActive(href) ? 'active' : '');
const homeActiveClass = () => (isHome ? 'active' : '');

const canonicalPath = canonical ? normPath(canonical) : currentPath;
const canonicalUrl = `${SITE_URL}${canonicalPath === '/' ? '/' : canonicalPath}`;
const metaKeywords = keywordsProp ?? getPageKeywords(canonicalPath, title);
const ogImageUrl = ogImage.startsWith('http') ? ogImage : `${SITE_URL}${ogImage.startsWith('/') ? ogImage : `/${ogImage}`}`;
const hasQueryParams = Astro.url.search.length > 0;
const isIndexable = !noindex && !hasQueryParams;
const robotsContent = isIndexable
  ? 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'
  : 'noindex, follow';
const ogTitle = title;
const ogDescription = description;

const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: SITE_NAME,
  url: SITE_URL + '/',
  description,
  inLanguage: 'en',
  publisher: { '@type': 'Organization', name: SITE_NAME, url: SITE_URL + '/' },
};

const siteNavSchema = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Main navigation',
  itemListElement: NAV_ITEMS.map((item, i) => ({
    '@type': 'SiteNavigationElement',
    position: i + 1,
    name: item.name,
    url: `${SITE_URL}${item.url === '/' ? '/' : item.url}`,
  })),
};

const orgLayoutSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: SITE_NAME,
  url: SITE_URL + '/',
  email: SITE_EMAIL,
  telephone: SITE_PHONE_TEL,
  sameAs: Object.values(SOCIAL_LINKS),
};
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="apple-touch-icon" href="/favicon.png" />
  <meta name="description" content={description} />
  <meta name="keywords" content={metaKeywords} />
  <title>{title}</title>
  <link rel="canonical" href={canonicalUrl} />
  <link rel="alternate" hreflang="en" href={canonicalUrl} />
  <link rel="alternate" hreflang="x-default" href={canonicalUrl} />
  <meta name="robots" content={robotsContent} />
  <meta name="googlebot" content={robotsContent} />
  <meta http-equiv="X-Frame-Options" content="SAMEORIGIN" />
  <meta name="referrer" content="strict-origin-when-cross-origin" />
  <!-- Open Graph (property + name for broad crawler support) -->
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content={SITE_NAME} />
  <meta property="og:title" content={ogTitle} />
  <meta property="og:description" content={ogDescription} />
  <meta property="og:url" content={canonicalUrl} />
  <meta property="og:image" content={ogImageUrl} />
  <meta property="og:image:secure_url" content={ogImageUrl} />
  <meta property="og:image:type" content="image/png" />
  <meta property="og:image:width" content={String(OG_IMAGE_WIDTH)} />
  <meta property="og:image:height" content={String(OG_IMAGE_HEIGHT)} />
  <meta property="og:image:alt" content={`${SITE_NAME} — ${ogTitle}`} />
  <meta property="og:locale" content="en_US" />
  <meta name="og:title" content={ogTitle} />
  <meta name="og:description" content={ogDescription} />
  <meta name="og:image" content={ogImageUrl} />
  <meta name="og:url" content={canonicalUrl} />
  <!-- Twitter / X Card (name + property for Ahrefs and all crawlers) -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta property="twitter:card" content="summary_large_image" />
  <meta name="twitter:site" content="@TranslatorOldEng" />
  <meta property="twitter:site" content="@TranslatorOldEng" />
  <meta name="twitter:creator" content="@TranslatorOldEng" />
  <meta name="twitter:domain" content="translatoroldenglish.com" />
  <meta name="twitter:url" content={canonicalUrl} />
  <meta name="twitter:title" content={ogTitle} />
  <meta property="twitter:title" content={ogTitle} />
  <meta name="twitter:description" content={ogDescription} />
  <meta property="twitter:description" content={ogDescription} />
  <meta name="twitter:image" content={ogImageUrl} />
  <meta property="twitter:image" content={ogImageUrl} />
  <meta name="twitter:image:src" content={ogImageUrl} />
  <meta name="twitter:image:alt" content={`${SITE_NAME} — ${ogTitle}`} />
  <link rel="image_src" href={ogImageUrl} />
  <link rel="alternate" type="application/rss+xml" title={`${SITE_NAME} Blog RSS`} href={`${SITE_URL}/rss.xml`} />
  <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(siteNavSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(orgLayoutSchema)} />
  <link
    rel="preload"
    href="/fonts/cinzel-600.ttf"
    as="font"
    type="font/ttf"
    crossorigin
  />
  <link
    rel="preload"
    href="/fonts/im-fell-english-italic.ttf"
    as="font"
    type="font/ttf"
    crossorigin
  />
  <slot name="head" />
</head>
<body class:list={[{ 'is-home': isHome }]}>

<div class="history-overlay" id="historyOverlay"></div>

<aside class="history-panel" id="historyPanel" role="complementary" aria-label="Translation history">
  <div class="history-header">
    <div>
      <p class="history-panel-title" style="margin-bottom:4px;font-weight:600;font-size:var(--fs-h3-h4);">Translation history</p>
      <p class="text-xs" style="color:var(--text-muted);">Last 50 translations · stored locally</p>
    </div>
    <button class="btn btn-ghost btn-sm" id="closeHistory" aria-label="Close history">✕</button>
  </div>
  <div class="history-list" id="historyList">
    <div class="history-empty">
      <div style="margin-bottom:12px;">📜</div>
      <p>No translations yet.<br/>Your history will appear here.</p>
    </div>
  </div>
  <div style="padding:16px;border-top:1px solid var(--border);">
    <button class="btn btn-ghost btn-sm" id="clearAllHistory" style="width:100%;">Clear All History</button>
  </div>
</aside>

<header class="site-header" role="banner">
  <div class="header-inner">
    <a href="/" class="site-logo" id="site-logo">
      <span class="logo-mark" aria-hidden="true">T</span>
      <span class="logo-text-wrap">
        <span class="logo-main logo-mobile logo-desktop">
          <span class="logo-line1">Translator</span>
          <span class="logo-line2"><span class="logo-accent">Old English</span></span>
        </span>
        <span class="logo-sub">Archaic language precision toolkit</span>
      </span>
    </a>

    <nav class="desktop-nav" role="navigation" aria-label="Main navigation">
      <ul class="nav-list">
        {NAV_ITEMS.map((item) => (
          <li>
            <a
              href={item.url}
              class:list={['nav-link', item.url === '/' ? homeActiveClass() : activeClass(item.url)]}
              aria-current={item.url === '/' ? (isHome ? 'page' : undefined) : (isActive(item.url) ? 'page' : undefined)}
            >
              {item.name}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="header-actions">
      <button class="icon-btn mobile-menu-btn" id="mobileMenuBtn" aria-label="Open menu" aria-expanded="false" title="Menu">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M4 7h16M4 12h16M4 17h16"/></svg>
      </button>
      <button class="hdr-btn" id="historyBtn" title="Translation History" aria-label="Open translation history">
        <span class="hdr-icon">📜</span>
        <span class="hdr-label">History</span>
      </button>
    </div>
  </div>

  <div class="mobile-nav" id="mobileNav">
    <nav>
      {NAV_ITEMS.map((item) => (
        <a
          href={item.url}
          class:list={['mobile-nav-link', item.url === '/' ? homeActiveClass() : activeClass(item.url)]}
        >
          {item.name}
        </a>
      ))}
    </nav>
  </div>
</header>

<main>
  <slot />
</main>

<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href="/" class="site-logo footer-logo">
          <span class="logo-mark logo-mark--sm" aria-hidden="true">T</span>
          <span class="logo-main">
            <span class="logo-line1">Translator</span>
            <span class="logo-line2"><span class="logo-accent">Old English</span></span>
          </span>
        </a>
        <p class="footer-tagline">
          Free AI-powered Old English translator — Early Modern, Middle, Anglo-Saxon, and simplified archaic styles.
        </p>
        <div class="footer-contact">
          <a class="footer-contact-link" href={`mailto:${SITE_EMAIL}`}>{SITE_EMAIL}</a>
          <a class="footer-contact-link" href={`tel:${SITE_PHONE_TEL}`}>{SITE_PHONE}</a>
        </div>
        <nav class="footer-social" aria-label="Social media">
          <a href={SOCIAL_LINKS.twitter} rel="noopener noreferrer" target="_blank" aria-label="X (Twitter)">X</a>
          <a href={SOCIAL_LINKS.facebook} rel="noopener noreferrer" target="_blank" aria-label="Facebook">Facebook</a>
          <a href={SOCIAL_LINKS.linkedin} rel="noopener noreferrer" target="_blank" aria-label="LinkedIn">LinkedIn</a>
          <a href={SOCIAL_LINKS.youtube} rel="noopener noreferrer" target="_blank" aria-label="YouTube">YouTube</a>
          <a href={SOCIAL_LINKS.instagram} rel="noopener noreferrer" target="_blank" aria-label="Instagram">Instagram</a>
          <a href={SOCIAL_LINKS.pinterest} rel="noopener noreferrer" target="_blank" aria-label="Pinterest">Pinterest</a>
          <a href={SOCIAL_LINKS.tiktok} rel="noopener noreferrer" target="_blank" aria-label="TikTok">TikTok</a>
        </nav>
        <p class="footer-meta-row">
          <a href="/rss.xml" rel="alternate">RSS feed</a>
          <span class="footer-meta-sep" aria-hidden="true">·</span>
          <a href={DMCA_URL} rel="noopener noreferrer" target="_blank">DMCA</a>
        </p>
      </div>
      <div class="footer-col">
        <p class="footer-col-title">Quick links</p>
        <ul class="footer-links">
          {FOOTER_QUICK_LINKS.map((item) => (
            <li>
              <a
                href={item.url}
                class:list={[item.url === '/' ? homeActiveClass() : activeClass(item.url)]}
                aria-current={item.url === '/' ? (isHome ? 'page' : undefined) : (isActive(item.url) ? 'page' : undefined)}
              >
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div class="footer-col">
        <p class="footer-col-title">Blog</p>
        <ul class="footer-links">
          {footerBlogLinks.map((post) => (
            <li>
              <a
                href={`/blog/${post.slug}/`}
                class:list={[activeClass(`/blog/${post.slug}/`)]}
                aria-current={isActive(`/blog/${post.slug}/`) ? 'page' : undefined}
              >
                {post.label}
              </a>
            </li>
          ))}
          <li><a href="/blog/" class:list={[activeClass('/blog/'), 'footer-see-all']} aria-current={isActive('/blog/') ? 'page' : undefined}>All articles →</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <p class="footer-copy">© 2026 Translator Old English. Free for personal and commercial use.</p>
      <div class="footer-bottom-links">
        <div class="footer-locale" id="siteLocaleWrap">
          <button type="button" class="footer-locale-btn" id="siteLocaleBtn" aria-label="Website language" title="Website language">
            <svg class="footer-locale-globe" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" aria-hidden="true">
              <circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/>
            </svg>
          </button>
          <select id="siteLocaleSelect" class="footer-locale-select" aria-label="Translate website">
            {SITE_LOCALES.map((loc) => (
              <option value={loc.code}>{loc.label}</option>
            ))}
          </select>
        </div>
        <a href="/privacy-policy/" class:list={['footer-meta-link', activeClass('/privacy-policy/')]}>Privacy</a>
        <a href="/terms-and-conditions/" class:list={['footer-meta-link', activeClass('/terms-and-conditions/')]}>Terms</a>
        <a href="/cookies/" class:list={['footer-meta-link', activeClass('/cookies/')]}>Cookies</a>
      </div>
    </div>
  </div>
</footer>

<button type="button" class="back-to-top" id="backToTop" aria-label="Back to top" title="Back to top">↑</button>

<script>
  /* No theme override needed — using default purple palette */

  if (document.body.classList.contains('is-home')) {
    if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
    const scrollHomeTop = () => {
      if (window.location.hash === '#tool-area') {
        history.replaceState(null, '', window.location.pathname + window.location.search);
      }
      window.scrollTo(0, 0);
    };
    scrollHomeTop();
    document.addEventListener('DOMContentLoaded', scrollHomeTop);
    requestAnimationFrame(() => {
      scrollHomeTop();
      requestAnimationFrame(scrollHomeTop);
    });
    window.addEventListener('load', scrollHomeTop);
    setTimeout(scrollHomeTop, 150);
    setTimeout(scrollHomeTop, 400);
    window.addEventListener('pageshow', (e) => { if (e.persisted) scrollHomeTop(); });
  }

  const mobileMenuBtn = document.getElementById('mobileMenuBtn');
  const mobileNav = document.getElementById('mobileNav');
  mobileMenuBtn?.addEventListener('click', () => {
    const open = mobileNav?.classList.toggle('open');
    mobileMenuBtn.setAttribute('aria-expanded', String(!!open));
  });

  const historyBtn = document.getElementById('historyBtn');
  const historyPanel = document.getElementById('historyPanel');
  const historyOverlay = document.getElementById('historyOverlay');
  const closeHistory = document.getElementById('closeHistory');
  function openHistory() { historyPanel?.classList.add('open'); historyOverlay?.classList.add('open'); renderHistory(); }
  function closeHistoryPanel() { historyPanel?.classList.remove('open'); historyOverlay?.classList.remove('open'); }
  historyBtn?.addEventListener('click', openHistory);
  closeHistory?.addEventListener('click', closeHistoryPanel);
  historyOverlay?.addEventListener('click', closeHistoryPanel);

  function renderHistory() {
    const list = document.getElementById('historyList');
    if (!list) return;
    const items: Array<{style:string;input:string;output:string;date:string}> = JSON.parse(localStorage.getItem('oe-history') || '[]');
    if (!items.length) {
      list.innerHTML = '<div class="history-empty"><div style="margin-bottom:12px;">📜</div><p>No translations yet.<br/>Your history will appear here.</p></div>';
      return;
    }
    list.innerHTML = items.map((item, i) => `
      <div class="history-item">
        <div class="history-item-style">${item.style}</div>
        <div class="history-item-text">${item.input}</div>
        <div class="history-item-actions">
          <button class="btn btn-ghost btn-sm" onclick="copyHistory(${i})">Copy</button>
          <button class="btn btn-ghost btn-sm" onclick="downloadHistory(${i})">Download</button>
          <button class="btn btn-ghost btn-sm" onclick="deleteHistory(${i})">Delete</button>
        </div>
      </div>
    `).join('');
  }

  (window as any).copyHistory = (i: number) => {
    const items = JSON.parse(localStorage.getItem('oe-history') || '[]');
    if (items[i]) navigator.clipboard.writeText(items[i].output || items[i].input);
  };
  (window as any).downloadHistory = (i: number) => {
    const items = JSON.parse(localStorage.getItem('oe-history') || '[]');
    const item = items[i];
    if (!item) return;
    const text = item.output || item.input || '';
    if ((window as any).downloadText) {
      (window as any).downloadText(text, 'translation-' + (item.style || 'history') + '.txt');
    }
  };
  (window as any).deleteHistory = (i: number) => {
    const items = JSON.parse(localStorage.getItem('oe-history') || '[]');
    items.splice(i, 1);
    localStorage.setItem('oe-history', JSON.stringify(items));
    renderHistory();
  };

  document.getElementById('clearAllHistory')?.addEventListener('click', () => {
    localStorage.removeItem('oe-history');
    renderHistory();
  });

  (window as any).saveToHistory = (style: string, input: string, output: string) => {
    const items: Array<{style:string;input:string;output:string;date:string}> = JSON.parse(localStorage.getItem('oe-history') || '[]');
    items.unshift({ style, input, output, date: new Date().toISOString() });
    if (items.length > 50) items.splice(50);
    localStorage.setItem('oe-history', JSON.stringify(items));
  };

  document.querySelectorAll('.faq-question').forEach(btn => {
    btn.addEventListener('click', () => {
      const item = btn.closest('.faq-item');
      item?.classList.toggle('open');
    });
  });

  const backToTop = document.getElementById('backToTop');
  if (backToTop) {
    const toggleBackToTop = () => {
      backToTop.classList.toggle('visible', window.scrollY > 400);
    };
    window.addEventListener('scroll', toggleBackToTop, { passive: true });
    toggleBackToTop();
    backToTop.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
  }
</script>
<script src={`${import.meta.env.BASE_URL}sentence-case.js`} defer></script>
<script src={`${import.meta.env.BASE_URL}translator.js`} defer></script>
<script src={`${import.meta.env.BASE_URL}speech-input.js`} defer></script>
{isHome && <script src={`${import.meta.env.BASE_URL}home-locale.js`} defer></script>}
</body>
</html>
